`
    Resumo:

        Adiciona tipagem estática no JavaScript,
        deixando o código muito mais seguro.

        Facilita o desenvolvimento nos dando feedback em tempo real.


    Preparação e instalação:

        npm install typescript --save-dev

        arquivo.ts // Extensão para arquivos TypeScript.

    Transpilar o código TypeScript:

        npx tsc arquivo.ts

        npx tsc --help // Para ver as opções de configuração.

        npx tsc --init // Para abrir as configurações do TypeScript.

        npx tsc -w // Ativar o modo watched

    
    Executar o TypesScript direto do terminal:

        npm i ts-node -D
`;



function add(nome?: string){ // Parâmetro opcional no TS
    
}

