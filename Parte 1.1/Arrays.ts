`
    Uma array é definida com o tipo de dado(s) que ela possui, seguida por [];
`;

const values: (string | number)[] = ["saa", 5454]; // Uma array de string ou number
const values2: number[] = [1,2,3,4,5];

//// Sintaxe Alternativa

const valores: Array<number> = [1,2,3,4,5];